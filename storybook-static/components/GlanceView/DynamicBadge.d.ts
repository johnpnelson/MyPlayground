import { default as React } from '../../../node_modules/react';
interface DynamicBadgeProps {
    badgeText: string;
    badgeStyles?: string;
    fieldIndex: number;
}
export declare const DynamicBadge: React.FC<DynamicBadgeProps>;
export {};
